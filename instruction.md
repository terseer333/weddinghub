

1. PREVIEW CARD

When the user is viewing the Preview Card and clicks "Return to Editor", it must automatically return directly to the Card Studio → Template section.

Do not redirect the user to the general dashboard or any other page.

The flow should be:

Card Studio → Template → Preview Card → "Return to Editor" → Card Studio → Template

Preserve the selected template and all previously entered wedding information when returning to the editor.


2. RECEPTION VENUE ON INVITATION CARD

The Reception Venue is currently not being displayed on the invitation card.

Add the Reception Venue to the card and make sure it is pulled automatically from the wedding information entered by the user.

Do not remove or change the existing venue information.

If a Reception Venue has been provided, it must appear clearly on the invitation card alongside the other wedding details.

If no Reception Venue has been provided, do not display an empty field or placeholder.

Only make this specific change. Do not modify unrelated parts of the card design or functionality.



3. PHOTO / STORY AND LOVE STORY — IMAGE UPLOAD

The Photo / Story and Love Story sections are currently not allowing users to add images or pictures.

Fix this functionality so users can upload and add images to both sections.

Requirements:
- Allow users to select and upload images from their device.
- Display the uploaded images properly in the Photo / Story section.
- Display the uploaded images properly in the Love Story section.
- Make sure the uploaded images are saved and remain available after refreshing the page.
- Preserve the existing text and story functionality.
- Do not remove or change existing features.

Only make this specific change. Do not go beyond my instructions or prompt.

4. SIGN OUT BUTTON

The "Sign Out" button should properly sign the user out of the WeddingHub website.

When the user clicks "Sign Out":
- End the user's current authenticated session.
- Clear the user's active authentication/session data.
- Prevent the user from accessing protected pages after signing out.
- Redirect the user to the appropriate login/sign-in page.

Only fix the Sign Out functionality.

Do not change or modify any unrelated features, pages, designs, or functionality.
