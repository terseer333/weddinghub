const DM=window.AdminModal;
document.getElementById('addEvent').onclick=()=>editEvent();
function editEvent(id){const item=AdminApp.data.events.find(event=>event.id===id)||{id:`evt_${Date.now()}`,name:'',date:'',time:'',venue:'',address:'',description:'',status:'draft'};DM.show(`<p class="eyebrow">Wedding schedule</p><h2>${id?'Edit':'Add'} event</h2><form id="eventForm" class="modal-form"><label>Event name<input name="name" value="${DM.field(item.name)}" required></label><div class="form-grid"><label>Date<input name="date" type="date" value="${DM.field(item.date)}" required></label><label>Time<input name="time" type="time" value="${DM.field(item.time)}" required></label></div><label>Venue<input name="venue" value="${DM.field(item.venue)}" required></label><label>Address<input name="address" value="${DM.field(item.address)}"></label><label>Description<textarea name="description" rows="3">${DM.field(item.description)}</textarea></label><label>Visibility<select name="status">${DM.statusOptions(item.status)}</select></label><button class="button primary" type="submit">Save event</button></form>`);document.getElementById('eventForm').onsubmit=async event=>{event.preventDefault();Object.assign(item,Object.fromEntries(new FormData(event.target)));if(!id)AdminApp.data.events.push(item);DM.close();await AdminApp.persist('Event saved')}}
document.getElementById('addStory').onclick=()=>editStory();
function editStory(id){const item=AdminApp.data.stories.find(story=>story.id===id)||{id:`story_${Date.now()}`,year:new Date().getFullYear().toString(),title:'',content:'',status:'draft',order:AdminApp.data.stories.length+1};DM.show(`<p class="eyebrow">Our love story</p><h2>${id?'Edit':'Add'} chapter</h2><form id="storyForm" class="modal-form"><div class="form-grid"><label>Year or date<input name="year" value="${DM.field(item.year)}" required></label><label>Order<input name="order" type="number" min="1" value="${item.order}" required></label></div><label>Chapter title<input name="title" value="${DM.field(item.title)}" required></label><label>Story<textarea name="content" rows="5" required>${DM.field(item.content)}</textarea></label><label>Visibility<select name="status">${DM.statusOptions(item.status)}</select></label><button class="button primary" type="submit">Save chapter</button></form>`);document.getElementById('storyForm').onsubmit=async event=>{event.preventDefault();Object.assign(item,Object.fromEntries(new FormData(event.target)));item.order=Number(item.order);if(!id)AdminApp.data.stories.push(item);DM.close();await AdminApp.persist('Story chapter saved')}}
document.getElementById('newAnnouncement').onclick=()=>editAnnouncement();
function editAnnouncement(id){const item=AdminApp.data.announcements.find(value=>value.id===id)||{id:`ann_${Date.now()}`,title:'',message:'',date:new Date().toISOString().slice(0,10),status:'draft'};DM.show(`<p class="eyebrow">Guest update</p><h2>${id?'Edit':'New'} announcement</h2><form id="announcementForm" class="modal-form"><label>Title<input name="title" value="${DM.field(item.title)}" required></label><label>Message<textarea name="message" rows="5" required>${DM.field(item.message)}</textarea></label><label>Visibility<select name="status">${DM.statusOptions(item.status)}</select></label><button class="button primary" type="submit">Save announcement</button></form>`);document.getElementById('announcementForm').onsubmit=async event=>{event.preventDefault();Object.assign(item,Object.fromEntries(new FormData(event.target)));if(!id)AdminApp.data.announcements.unshift(item);DM.close();await AdminApp.persist(item.status==='published'?'Announcement published':'Announcement saved')}}
function readImage(file){return new Promise((resolve,reject)=>{if(file.size>700000)return reject(new Error('Choose an image smaller than 700 KB'));const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=()=>reject(new Error('Could not read this image'));reader.readAsDataURL(file)})}
document.getElementById('addPhoto').onclick=()=>{DM.show(`<p class="eyebrow">Wedding gallery</p><h2>Add a photo</h2><p class="modal-intro">Upload a compressed image up to 700 KB, or use a direct HTTPS URL.</p><form id="photoForm" class="modal-form"><label>Upload image<input name="file" type="file" accept="image/jpeg,image/png,image/webp"></label><div class="form-divider"><span>or</span></div><label>Image URL<input name="url" type="url" placeholder="https://your-cdn.com/photo.jpg"></label><label>Caption<input name="caption" required></label><label>Visibility<select name="status">${DM.statusOptions('published')}</select></label><button class="button primary" type="submit">Add to gallery</button></form>`);document.getElementById('photoForm').onsubmit=async event=>{event.preventDefault();const form=new FormData(event.target),file=form.get('file');let url=String(form.get('url')||'').trim();try{if(file&&file.size)url=await readImage(file);if(!url)throw new Error('Upload an image or enter an image URL');const photo={id:`photo_${Date.now()}`,url,caption:String(form.get('caption')),status:String(form.get('status'))};AdminApp.data.photos.push(photo);if(AdminApp.data.photos.length===1||!AdminApp.data.wedding.heroImage)AdminApp.data.wedding.heroImage=url;DM.close();await AdminApp.persist('Photo added')}catch(error){WH.toast(error.message)}}};
document.addEventListener('click',async event=>{const button=event.target.closest('[data-action]');if(!button)return;const {action,id}=button.dataset;if(action==='edit-event')return editEvent(id);if(action==='edit-story')return editStory(id);if(action==='edit-announcement')return editAnnouncement(id);const collections={'delete-event':'events','delete-photo':'photos','delete-story':'stories','delete-announcement':'announcements','delete-message':'messages'};if(collections[action]){if(!confirm('Remove this item?'))return;AdminApp.data[collections[action]]=AdminApp.data[collections[action]].filter(item=>item.id!==id);return AdminApp.persist('Item removed')}const message=AdminApp.data.messages.find(item=>item.id===id);if(!message)return;if(action==='approve-message')message.status='approved';if(action==='hide-message')message.status='hidden';await AdminApp.persist('Message moderation updated')});